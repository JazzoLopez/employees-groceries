<!DOCTYPE html>
<html>
<head>
  <title>Sistema de Abarrotes - Contacto</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://code.jquery.com/mobile/1.4.5/jquery.mobile-1.4.5.min.css">
  <script src="https://code.jquery.com/jquery-1.11.3.min.js"></script>
  <script src="https://code.jquery.com/mobile/1.4.5/jquery.mobile-1.4.5.min.js"></script>

  <style>
    body {
      background-color: #fff;
    }

    .ui-content {
      padding: 15px;
    }

    .contact-card {
      width: 80%;
      margin: 0 auto;
      background-color: #fff;
      padding: 10px;
      margin-top: 20px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .contact-avatar {
      width: 100px;
      height: 100px;
      margin: 0 auto 10px;
      overflow: hidden;
    }

    .contact-avatar img {
      width: 100px;
      height: auto;
    }

    .contact-name {
      text-align: center;
      font-size: 20px;
      margin-bottom: 5px;
    }

    .contact-title {
      text-align: center;
      font-size: 16px;
      color: #666;
      margin-bottom: 10px;
    }

    .contact-info {
      text-align: center;
    }
  </style>
</head>
<body>

<!-- Página de Contacto -->
<div data-role="page" id="contactos">

  <!-- Encabezado con barra de navegación -->
  <div data-role="header">
    <p style="text-align: center;">El mejor de todos fue...</p>
    <div data-role="navbar">
      <ul>
        <li><a href="/" data-icon="home" >Inicio</a></li>

        <li><a href="#contactos" data-icon="phone" class="ui-btn-active">Contactos</a></li>
      </ul>
    </div>
  </div>

  <!-- Contenido -->
  <div data-role="content">

    <!-- Tarjeta de Presentación -->
    <div class="contact-card">
      
      <div class="contact-name">El mejor empleado fue: <b><%= employee.name%> </b></div>
      <div class="contact-title">Felicidades por estar en la clasificacion numero <b><%= employee.rating %></b></div>
      <div class="contact-info">
        <!-- datos del empleado -->
        <p>Numero de trabajador: <%= employee.workerNumber %></p>
        <p>Departamento: <%= employee.department %></p>
        <p>Edad: <%= employee.age %></p>
        <p>Salario: <%= employee.salary %></p>
        <p style="font-size: larger; color: darkslategray; <% if(employee.rating >= 8 && employee.rating <= 10) { %>font-weight: bold; color: green;<% } else if (employee.rating >= 5 && employee.rating <= 7) { %>font-weight: normal; color: yellow;<% } else { %>font-weight: bold; color: red;<% } %>">Clasificación: <%= employee.rating %></p>


        
        <p></p>
      </div>
    </div>

  </div>

  <!-- Pie de página -->
  <div data-role="footer" data-position="fixed">
    <p style="text-align: center;">Github/JazzoLopez </p>
  </div>
</div>

</body>
</html>
